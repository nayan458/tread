module.exports = {
  semi: true, // Use semicolons at the end of statements.
  singleQuote: true, // Prefer single quotes over double quotes.
  trailingComma: 'es5', // Add trailing commas in ES5-compliant syntaxes (e.g., objects, arrays).
  printWidth: 80, // Wrap lines at 80 characters for better readability.
  tabWidth: 2, // Use 2 spaces for indentation.
  useTabs: false, // Use spaces instead of tabs.
  arrowParens: 'always', // Always include parentheses around arrow function arguments.
  bracketSpacing: true, // Add spaces inside brackets.
  jsxBracketSameLine: false, // Place the `>` of multi-line JSX tags on a new line.
  endOfLine: 'lf', // Use Line Feed (`\n`) for line breaks.
};
